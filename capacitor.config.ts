import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.duoc.start',
  appName: 'evaluacion1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
